<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="userEdu">
	
	<resultMap type="com.topia.card.User.UserVO" id="edu">
		<result column="EDU_IDX" 			property="eduIdx"/>
		<result column="USER_IDX" 			property="userIdx"/>
		<result column="EDU_SCHOOL_NAME" 	property="eduSchoolName"/>
		<result column="EDU_STATUS" 		property="eduStatus"/>
		<result column="EDU_YEAR" 			property="eduYear"/>
		<result column="EDU_MONTH" 			property="eduMonth"/>
	</resultMap>
	
	<insert id="insert">
		<foreach collection="eduList" item="item" index="index" open="INSERT ALL" separator=" " close="SELECT * FROM DUAL">
		INTO USER_INFO_EDU
			(
			 EDU_IDX
			, USER_IDX
			, EDU_SCHOOL_NAME
			, EDU_STATUS
			, EDU_YEAR
			, EDU_MONTH
			)
		VALUES
			(
			 USER_INFO_EDU_SEQ.NEXTVAL+#{index}
			, USER_INFO_SEQ.CURRVAL
			, #{item.eduSchoolName}
			, #{item.eduStatus}
			, #{item.eduYear}
			, #{item.eduMonth}
			)
		</foreach>
	</insert>
	
</mapper>